---
description: 在用户要求创建 git 提交、写中文 commit message、按项目 Conventional Commits 规范提交，或 @git-commit-cn 时应用；规定 type/scope/中文简述格式、Husky 校验与提交流程。
alwaysApply: false
---
# Git 提交规范（中文）

**在用户要求提交代码、按本规范写 message，或 @ 提及 `git-commit-cn` 时执行。**

## 提交信息格式

```
<type>(<scope>): <中文简述>
```

- **type**（必填）：`feat` | `fix` | `docs` | `chore` | `refactor` | `perf` | `test` | `build` | `style`
- **scope**（推荐）：`tauri` | `frontend` | `ui` | `animation` | `build` | `deps` | `env` | `data` | `skills` 等，与改动模块一致
- **简述**：简体中文，一句话说清「为什么 / 做了什么」，不用英文句子

### 示例（与本仓库历史一致）

```
feat(ui): 重构下载进度、工具卡片与标题栏
feat(tauri): 支持 downloadKey 解析与 Windows zip 分步安装
chore(deps): 增加动画相关依赖
docs: 补充工具清单字段说明
```

## 自动化校验（Husky）

- **pre-commit**：先执行 `bun run format`（Prettier + `cargo fmt`），将格式化后的已跟踪文件 `git add -u` 回暂存区，再执行 `bun run lint`（全量 ESLint）；任一步失败则无法提交
- **commit-msg**：由 `@commitlint/config-conventional` 校验，message 必须以 `feat` / `fix` / `chore` 等 type 开头，格式为 `<type>(<scope>): <简述>`

跳过校验需显式使用 `git commit --no-verify`（默认禁止）。

## 执行提交时的流程

1. 并行查看：`git status`、`git diff`、`git log -10 --oneline`（对齐既有风格）
2. 根据**全部**待提交变更归纳一条 subject；大范围改动可拆多个 commit，默认用户未要求则单次提交
3. **不要**提交 `.env`、密钥、token 等敏感文件；若用户坚持，先警告
4. `git add` 相关文件 → `git commit -m "..."` → `git status` 确认成功
5. 未要求时 **不要** `git push`

## 禁止

- 英文为主的 commit message（如 `Add modal window`）
- 无 type 的随意描述（如 `update`、`fix bug`）
- 跳过 hooks（`--no-verify`）除非用户明确要求
