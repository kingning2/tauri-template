---
description: 前端编写规范 — 目录职责、enums、invokeWrapper、Redux、布局与 TS；完整版见 docs/development-rules/frontend.md
globs:
  - src/**/*
alwaysApply: false
---

# 前端编写规范（摘要）

完整文档：[`docs/development-rules/frontend.md`](../../docs/development-rules/frontend.md)

## 必守

- IPC / 路由 / 语言 / 窗口 / Modal / Redux slice 等**固定字符串** → `@/enums`；**用户可见文案** → i18n JSON。
- Tauri 调用只经 `src/cmd` + `invokeWrapper(TauriCmd.Xxx)`；禁止组件内裸 `invoke('...')`。
- 日志用 Event（`FeLog`），勿新增 log 类 invoke。
- 跨 Webview 会话源真相在 Rust `context/`；Redux 镜像 + `TauriEvent.SessionChanged` / `get_app_session`。
- 主窗布局：`min-h-0` + `overflow-hidden` 链；滚动只给最内层业务区。
- IPC 类型：`@/generated/contracts`；`contracts.ts` 禁止手改。
- 动画放 `src/animation/`；尊重 `prefers-reduced-motion`。

## 反例

```ts
// ❌ 魔法字符串、裸 invoke
await invoke('set_lang', { lang: 'en' })
dispatch(changeCurrentLanguageAction('en' as any))

// ✅
import { Language, TauriCmd } from '@/enums'
await invokeWrapper(TauriCmd.SetLang, { lang: Language.En })
dispatch(changeCurrentLanguageAction(Language.En))
```
